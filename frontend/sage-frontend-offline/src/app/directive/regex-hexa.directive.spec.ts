import { RegexHexaDirective } from './regex-hexa.directive';

describe('RegexHexaDirective', () => {
  it('should create an instance', () => {
    const directive = new RegexHexaDirective();
    expect(directive).toBeTruthy();
  });
});
